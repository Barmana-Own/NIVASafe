# Reports

FMEA PDF and XLSX files are generated by the API after tenant authorization. Names follow `NIVASafe-FMEA-{projectCode}-{assessmentCode}-{date}`. Spreadsheet rows contain scores and risk level. Print CSS repeats table headers and avoids row breaks. Persian production PDFs should mount an approved Persian font and register it with PDFKit.
