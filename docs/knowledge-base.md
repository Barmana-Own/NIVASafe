# Knowledge base

Documents are private to an organization, searchable by title/content and tagged. Only published documents can ground assistant answers. Every create is audited. Future chunk/vector indexing must keep `organizationId` in each chunk and query filter.
