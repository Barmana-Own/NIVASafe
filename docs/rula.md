# RULA

RULA inputs are validated and calculated by the shared data-driven domain engine; the server is authoritative and returns score 1–7, action level 1–4 and a calculation trace. UI drafts are kept in IndexedDB and the result explicitly remains subject to HSE expert review.
