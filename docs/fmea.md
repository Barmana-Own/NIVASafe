# FMEA

Severity, occurrence and detection are integers 1–10. The API calculates `RPN = S × O × D`; clients cannot submit RPN. Risk bands default to 50/100/200 and are organization-configurable. Rows retain controls, recommendations and residual-score fields. Excel/PDF downloads re-check tenant membership before generation.
